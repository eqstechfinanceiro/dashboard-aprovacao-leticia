[phases.setup]
nixPkgs = ["nodejs", "python3"]

[phases.build]
cmds = ["cd vexpenses-dashboard && npm install && npm run build"]

[start]
cmd = "cd vexpenses-dashboard && npm run start"

[variables]
NODE_ENV = "production"
